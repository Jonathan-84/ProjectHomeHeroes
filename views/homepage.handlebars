<ol class="task-list">
  <li>
    <article class="kids">
      <div class="title">
        <a href="{{post_url}}" target="_blank">{{title}}</a>
        <span>({{post_url}})</span>
      </div>
      <div class="meta">
        {{current_points}} point(s) earned by {{child_name}}, this will be added to 
       their banked total of {{banked_points}}. Their guaradian {{user.name}}.
        |
        <a href="/post/{{id}}">{{tasks.length}} task(s)</a>
      </div>
    </article>
  </li>
</ol>